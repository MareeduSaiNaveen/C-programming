#include<stdio.h>
int main(){
	printf("Enter a size of row : ");
	scanf("%d", &row);
	printf("Enter a size of col : ");
	scanf("%d", &col);
	int arr[row][col];
	for(i = 0; i < row; i++){
		for(j = 0; j < col; j++){
			scanf("%d", &arr[i][j]);
		}
	}for(i = 0; i < row; i++){
		for(j = 0; j < col; j++){
			arr1[k] = 
		}
	}
